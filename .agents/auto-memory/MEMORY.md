- [student_time_slot scaling (10gb pain table)](project_student_time_slot_scaling.md) — 13.9M-row planner table; parked Timescale-hypertable + clean composite-PK plan in `.agents/plans/`
- [Scoped cache planner adoption (goal)](project_scoped_cache_planner_adoption.md) — enable scoped mode in planner, scope field=student (user_created interim, later multi-owner sharing/teachers); real blocker = reads pin via relational paths not root scalar (must reshape to filter user_created at root); $CURRENT_USER pin VERIFIED SAFE (sanitizeQuery resolves it pre-service); multi-owner needs cache.scope/purge filter pair; relation over-invalidation PARKED (measure-first: pin root scalar → measure → only then filter-pair for hottest relation)
- [SQL query cache + partition tagging (PARKED)](project_directus_sql_query_cache_parked.md) — below-response SQL cache not worth it now; real follow-ups = per-student partition tags + owner-tagging for sharing; DB structural fix first
- [Query-filter $NOW is NOT a cache poison](reference_directus_query_now_not_cache_poison.md) — sanitizeQuery resolves $NOW→Date before the key is built (path=pathname-only, query=sanitizedQuery) → keys differ per request → no stale HIT; mis-analysed ≥2×; only permission-side $NOW is staleness (filter not keyed); #291 gate is HYGIENE (never-hit entry bloat), not a fix
- [Create-takeover cache purge is coarse-by-default](reference_directus_takeover_cache_scoping.md) — a create hook takeover purges COARSE unless it declares its footprint via scopedCache.purgeBy (renamed from addTag); narrowing unconditionally (#293, closed) poisons the old slice of an upsert-move (createMany has no old∪new capture, post-commit snapshot sees only NEW); NOT symmetric with normal creates; gate = someRowTakenOver && no declared tags → null; takeover(PK)→coarse net, cancel(null)→no net (must declare)
- [scopedCache hook API (scopeTo/purgeBy)](reference_directus_scopedcache_api.md) — #292 CRUD-filter channel: read→scopedCache.scopeTo, mutation→scopedCache.purgeBy (single union field, one method wired per event); pass result.getMeta().scopedCacheTags to reuse pinned slices; full compile-time event→method typing deferred to #294 (event-parameterize EventContext/FilterHandler)
- [No more upstream PRs; fork additions under jean's BSL](project_directus_fork_no_upstream_prs.md) — free to diverge (edit packages/types, change public contracts); upstream-mergeability no longer a goal; LICENSE.fork = jean's own BSL-1.1, Additional Use Grant None, production grant on request
- [Scoped cache invalidation design (PR #203 + #205 value layer)](project_directus_scoped_cache_design.md) — getMeta() rider over instance field, override audit (Files/Permissions), ioredis+@keyv/redis-v5, blackbox label gating; #205 adds ScopedCacheTag value slices (scoped_cache_fields, bare=global readers, old∪new, cache.scope/purge filters)
- [#205 patch coverage — RESOLVED 2026-07-01](project_directus_scoped_cache_patch_coverage_parked.md) — was ~40% parked; closed IN-branch to ~99% (target bumped 90→95), gap was mostly reformatting-inflation + controllers/items.ts unit tests; codecov v2 commit-report API gives per-line misses
- [Scoped cache follow-ups: post-hook write fix + permission-case read scoping](project_directus_scoped_cache_permission_scoping.md) — #205 option-1 (purge tags off post-hook payload, takeover→full-flush) landed on work-pr205; branch A scopes reads off injected `ast.cases` (single-case bounds via joinFilterWithCases) to fix the planner permission-isolation gap; both local-only; AST-keyed cache is a dead end (perms n-join cached via withCache, perm changes already cache.clear)
- [get-schema raw-knex reads JSON meta dialect-native](project_directus_get_schema_raw_knex_json.md) — get-schema bypasses the cast-json pipeline → JSON meta cols come back parsed on PG, string on mysql/sqlite; normalize with @directus/utils parseJSON + array passthrough
- [Worktree vitest barrel/stream-json fail](reference_directus_worktree_vitest_barrel.md) — symlinked-node_modules worktree can't resolve stream-json subpath via services barrel; mock the barrel-reaching import or import concrete module
- [Inspect planner DB via pn db:query:local](reference_pn_db_query_local.md) — `./data/db_query_local.sh "<SQL>"` in planner_2/apps/directus → non-interactive psql in hhh-database container; **HARD RULE: NEVER run ANY mutating query in production** (`pn db:query:railway`) — SELECT-only on prod, mutations only in the local docker sandbox
- [Directus DB clients & batch-insert RETURNING support](project_directus_db_clients.md) — canonical list in
  `api/src/types/database.ts`; MariaDB rides under `mysql`; mysql/mariadb/sqlite hit the fallback loop
- [PR title naming convention](project_directus_pr_title_naming.md) — title prefix is FUNCTIONAL: compose-hhh-v11.yml
  greps `startswith("v11.10.1-feat: ")` to select composed features; classify by composed (→ `v11.10.1-feat:`,
  mandatory) vs merged-into-prepare (→ `ci()/chore(deps)`), NOT by which files it touches; NEVER commit prefix
  `fork-feat:` (#190 double-mistake); PUSHED branch = `v11.10.1-feat/<name>` not local `fork-feat/*-v11` (#202→#203 redo)
- [Amending the prior commit is OK on this feature branch](feedback_amend_ok.md) — for upstream-bound PR work, prefer
  `git commit --amend` + `git push --force-with-lease` over layered fix-up commits
- [Don't ask before pnpm install / build in this repo](feedback_no_ask_for_build_install.md) — project override of the
  global ask-before-install rule; routine install/build steps run silently, dep-modifying commands still ask
- [Never run blackbox locally — run it in CI](feedback_directus_bb_tests_in_ci.md) — local is SLOWER (full dist deploy +
  a server per env variant, no sharding) and re-seeds jean's docker stack; push, label `Run Blackbox`, loop on the run
- [Fork integration branches](project_directus_fork_integration_branches.md) — ⚠️2026-07-27 pr-controle RETIRED (tag archive/pr-controle), `v11.10.1-hhh-dev` IS now default; control-plane curated-ported (#303/#305), compose+publish parked; main = clean upstream base; hhh-main = derived from the copy stack; supersedes old
  main-overlay model; blackbox/e2e label-gated; blackbox-pr.yml is name:Check; CI gates = build+eslint+stylelint (no
  tsc)
- [Compose copy-stack architecture](project_directus_compose_copy_stack.md) — upstream-draft PRs isolated for upstream;
  parallel hhh-main-root/stacked copies resolve overlaps once; compose consumes the copies; SSH deploy key for
  workflow-file pushes; cla-bot reads contributors.yml from head ref → inject hhh-bot
- [Stacked copy rebuild gotchas](project_directus_stacked_rebuild_gotchas.md) — cherry-pick branches independent (fix
  low → rebuild above); divergent stacked history ≠ rebase (#52); merge-tree forced-base lies when behind main;
  test-file two-block merge must close the first block; semantic interactions (#50 vs #58) only surface in the full api
  test
- [Directus v12 license → dual-target compose](project_directus_v12_license_dual_compose.md) — v12+ needs a license key;
  v11.9.2 = last license-free release Hippocast runs; fork keeps last-v11 + main lines; compose infra (#64/#65) targets
  main only for now, last-v11 is a deferred phase; `upstream-diff:` = fork-permanent, lands on main, never upstream
- [Compose stack order rubric](project_directus_compose_stack_order_rubric.md) — hhh-main rebased-copy tree: roots =
  only isolated PRs; order root→leaf bugfix → perf → light contract consistency (payload/reasons) → contract changes
  (light→heavy) → refused-upstream; deps override rank only when a branch carries another's code
- [knex >=3.2 breaks Directus deep sort](project_directus_knex_deepsort_regression.md) — knex 3.2.10 #6392 wraps window
  aliases; Directus pre-wraps directus_row_number → double-escape → 500 on every o2m/m2m/m2a sort; pin knex 3.1.0
- [Codecov per-package flags; blackbox not in api unit flag](project_directus_codecov_flags.md) — codecov/patch/<pkg>
  target=auto≈project; blackbox-only api code fails patch/api → needs knex-mock-client unit tests; constants barrel line
  needs import-through-index
- [Directus ForbiddenError conventions](project_directus_forbidden_error_conventions.md) — 403 for missing items
  (anti-enumeration), pre-access validation → 400, ForbiddenError({reason}) is upstream + always-visible, status never
  env-gated; PRs #61/#62
- [api vitest typecheck blocked by pre-existing debt](reference_api_typecheck_blocked.md) — \*.test-d.ts never run;
  enabling typecheck surfaces ~203 src type errors (and emitter graph pulls in untyped pino-http-print) → needs a
  dedicated cleanup PR
- [Directus blackbox run + logs](project_directus_blackbox_run_and_logs.md) — trigger label-gated blackbox on a branch HEAD via compare-PR + Run Blackbox label; REST job logs lack containerized vitest output (read before ELIFECYCLE); never run bb locally as a pre-push gate — CI fans out 8 free shards, local is serial
- [Blackbox/integration coverage](project_directus_blackbox_coverage.md) — rolldown-plugin-istanbul instruments tsdown api build (gated COVERAGE_DIR), server dumps __coverage__ on terminus shutdown, CI merges→lcov→`-F blackbox`; needs tsdown so can't reach the tsc-built runtime feat stack until recompose
- [Linear chain + leaf PR (compose retired)](project_directus_linear_chain_leaf_pr.md) — v11 feats are now one linear chain #115→#114→#190→#98…#110; hhh-dev=prepare tip, PR #192 leaf→hhh-dev replaces compose; compose-hhh-v11 cron DISABLED
- [New feats base on v11.10.1-hhh-dev](feedback_directus_new_feat_base_hhh_dev.md) — hhh-dev is now THE work branch for new features (no more v11.10.1-feat/* chain branches); always `git fetch` + sync hhh-dev to its tip before starting a feat
- [Contributor Check needs GH-associated author](project_directus_contributor_check_author.md) — author commits as `1556489+jclaveau@users.noreply.github.com` NOT comptes.jc@gmail.com; else "PR contains commits without associated GitHub users"
- [Scalabus publish plan (PARKED)](project_scalabus_publish_plan.md) — publish fork as @scalabus/* to GitHub Packages npm for faster planner install; locked: GH-Packages registry (auth on install), publish-time rename (@directus→@scalabus, source untouched), own independent semver + per-pkg "upstream" field, tgz quick-win first
- [Port-audit: check NEW baseline too](reference_port_audit_check_new_baseline.md) — fork-delta-only audit gives false MISSING; upstream-merged PRs (jean's #25474 sdk-message in v11.10.1, #23907 readByQuery in both) are absorbed into the newer base tag — diff vs baseline, compare PR-merge-date vs tag-date
- [Never reset --hard a dirty tree](feedback_never_reset_hard_dirty_tree.md) — cleaning a --no-commit dry-run with reset --hard nukes pre-existing unstaged mods (no blob → unrecoverable); use --abort / scoped restore; recover via sibling-branch blobs or session-transcript Edit/Write content
- [rolldown 1.1.x moved `define` under `transform`](reference_rolldown_110_define_moved.md) — rc.x→1.1 bump breaks extensions-sdk build.ts with TS2769 ('define' not in ConfigExport); use `transform: { define: {...} }`
- [Blackbox coverage exit-0 teardown crash](reference_blackbox_coverage_exit0_teardown.md) — COVERAGE_DIR builds gracefully exit servers code 0 (terminus); upstream setup.ts throws on non-null exit → fix `code !== null && code !== 0`
- [Stacked-chain dep bump lockfile traps](project_directus_stacked_chain_dep_bump.md) — catalog dep bump in the feat chain: regen lockfile only from a branch's own consistent state (else 5000-line graph drift); catalog line adjacent to istanbul context-reverts on rebase → reconcile per dep-touching branch
- [directus emitFilter same-ref trap](reference_directus_emitfilter_same_ref.md) — emitFilter returns the SAME array ref with no listener; `x.length=0`+refill aliases & wipes it (PR#205 scoped-cache reads never tagged → stale HIT); reassign don't mutate-in-place
- [Blackbox cache HIT/MISS local repro](project_directus_blackbox_cache_local_repro.md) — repro a scoped-cache bug with one hand-run server (docker redis + sqlite bootstrap + `cli start` on spare port + curl + `keys 'ns:tag:*'`); gotchas: port 8055=jean's planner, pkill-by-env fails (kill by port), check dist staleness
- [Scoped cache tag derivation](project_directus_scoped_cache_tag_derivation.md) — what a read gets tagged with (root + relations via fields/filter/sort/aggregate; M2A over-tags allowed set; m2m/m2a target only on deep read; value-slice root-scalar only); only stale vector = enrichment outside the AST
- [Blackbox seed mechanics + no-link trick](project_directus_blackbox_seed_mechanics.md) — seedDBStructure auto-run via globby in seed-database.test.ts; CreateFieldM2O/O2M/M2M/M2A helpers + deep-read field paths; cache-invalidation tests need NO data linking (tags come from AST)
- [check.yml trigger scoping + Check ambiguity](reference_directus_check_workflow_scoping.md) — check.yml (Unit/Format/Lint/Style) fires only on prepare/feat** base PRs, NOT hhh-dev → verify hhh-dev-based PRs (#205) locally; two workflows named "Check" (blackbox-pr.yml + check.yml); cancel-in-progress makes a superseded run show "cancelled" rc=1 (false fail — watch current-head run). FIXED 2026-06-30: run on every commit (unfiltered push) → unblocks unit-coverage upload for codecov
- [Scalabus diff-scoped eslint style gate](project_directus_scalabus_style_gate.md) — eslint.style.config.js (planner config, prettier disabled, indent/semi/quotes/quality-rules off) + lint-style-changes.mjs (config-is-source, added-lines-only, untracked-file scan, tooling-exempt); two-pass --fix to satisfy both style + directus padding; custom arrow-multiline-block rule
- [Scoped cache value-scoping hardening (#205)](project_directus_scoped_cache_value_hardening.md) — snapshot committed rows NOT payload (DB default/trigger/coercion diverge); purgeCollectionScope SCAN fallback (not cache.clear); self-ref guard (root at >1 field-map path → bare); `\x00null` sentinel (7/"7" collapse intentional); PK-strip makes the old-key-snapshot edge impossible; only coarse purge left = hook-takeover
- [Blackbox value-slice cache coverage](project_directus_blackbox_value_scope_coverage.md) — scoped_cache_fields via CreateCollection meta at seed-structure time (before boot); strict-witness HIT/MISS design (assert the flipping value); upsertMany has no REST surface (unit-only); pinned reads tag slice-only not bare
- [#205 accepted exceptions — do NOT re-raise](project_directus_pr205_accepted_exceptions.md) — settled in review: big scope OK, String/number collapse OK but bool/date/decimal now type-normalized, relations bare-tagged by design, aggregate-bare NOT a bug, reader/writer race + parseJsonFieldList silent-degrade DEFERRED, CACHE_TTL false-alarm, $CURRENT_USER pin safe
- [Two eslint configs (correctness + style gate)](project_directus_two_eslint_configs.md) — CI splits Lint (eslint.config.js, full rules incl padding-line/newline-per-chained-call) from Style-changes (eslint.style.config.js diff-scoped max-len); style gate alone misses correctness fails → run BOTH locally before push; switch case-blocks after fall-through trip padding-line → use if-returns
- [Service-level read-through cache + CACHE_TYPES](project_directus_service_cache.md) — readByQuery caches via service-cache.ts (dual-write, own key namespace, HTTP fast-path intact); CACHE_TYPES array selects api/service consumers; settled decisions (system-collection excluded for security, TTL shared, gql out-of-scope) so review doesn't re-litigate; PR #207
- [Permission-case scoped cache (PR #212)](project_directus_permission_case_cache.md) — pin scoped-cache read tags off ast.cases (permission policy), not just the API filter; generalized `_or` in the filter pinner (union iff all branches bind); reuse joinFilterWithCases; settled: cases pre-resolved so no resolver, DNF rejected, no `_not`, root-cases-only. MERGED.
- [Style (changes) ≠ Lint job](reference_directus_style_changes_vs_lint_split.md) — the changed-line Style gate is WIDTH-only; full-lint rules (padding-line-between-statements) fire in the separate Lint job → a 90-col wrap can pass Style(changes), fail Lint; run full `eslint <file>` after any width wrap
- [Blackbox test sharding](project_directus_blackbox_sharding.md) — vendor×shard matrix; `before` in every shard, `after` DISTRIBUTED (barrier counts back from the shard's own afterFiles or it hangs); ordered after-chains as nested arrays; MEASURED ms weights not byte size; seeds a file BORROWS must follow it or 403; build-caching is a NET LOSS with parallel shards
- [Batch API ops in blackbox seeds](project_directus_blackbox_batch_seeds.md) — prefer CreateCollections (array POST → createMany, fields folded into the collection payload) / CreateItem array bodies over per-entity loops; no batch collection DELETE endpoint so cleanup stays 1-by-1
- [bb extension imports resolve from the extension dir](reference_directus_bb_extension_imports.md) — importing @directus/* in a bb endpoint extension needs it declared in tests/blackbox/package.json, else the extension load throws → route 404s → setDirectusEnv tests cascade-fail
- [@directus/env casting internals](reference_directus_env_casting.md) — type-map (per-key/regex) then guessType; process-env always cast (create-env:45); unmapped numeric env vars DO become numbers; guessType skips leading-zero values
- [Directus REST batch-delete matrix](reference_directus_rest_batch_delete.md) — items batch-delete YES (array/keys/query body); schema (collections/fields) NO (single only); collections DO batch-CREATE
- [bb "Port in use" bootstrap flake](reference_directus_blackbox_port_flake.md) — a shard failing at server bootstrap with "Port NNNNN already in use" is a runner port-collision flake (not a code bug — that'd fail all shards); `gh run rerun --failed`
- [DB connection priority (PR #213)](project_directus_db_connection_priority.md) — policy grants ranked DB connections; DatabasePoolExhaustedError (429+reason, per-dialect detection); settled decisions (priority on connection, dup-name throws, 429-not-503); CreateItem already batches — MAX_BATCH loops are intentional
- [Cache admin page + TTL telemetry (PR #227)](project_directus_cache_admin_page.md) — path→method+query→item tree, drawer w/ live Redis (tags/SCARD blast-radius/timestamps/size/recommended-TTL), pagination; Redis-stream→PG star schema (events fact + descriptors dim); recommended TTL = p95 re-request age; ACCEPTED (don't re-raise): content-type dropped (HIT=res.json), client-side filter (/items bans directus_*), p95/±25% knobs, group agg=max
- [/items forbids directus_* system collections](reference_directus_items_forbids_system_collections.md) — 403 "Forbidden access to directus_* collections"; a custom system collection isn't /items-readable → no native server-side filter, evaluate the Directus filter client-side (or rename to a non-directus_ user collection)
- [Blackbox flakes](project_directus_blackbox_flakes.md) — intermittent m2o.test.ts WebSocket-OPEN timeout (harness, not your diff); rerun failed shard via `gh run rerun <run-id> --failed` (only after run completes); codecov/patch reads red on partial data before all shards upload
- [Cache namespaces (PR #225)](project_directus_cache_namespaces.md) — root system-cache→scalabus (fork's new name); layers scalabus_response/_system/_schema/_lock, tag index scalabus:tag:*; scoped purge transparent to the _response split (tags key off root, Keyv prefixes members); CACHE_STORE narrowed to Store at the boundary
- [Env TYPE_MAP coverage](project_directus_env_type_map.md) — jean wants as many env vars as possible registered in TYPE_MAP (packages/env/src/constants/type-map.ts); it's a runtime coercion registry (not TS types — Env stays Record<string,unknown>); register every var you touch BEFORE review, parity with siblings, even plain strings get 'string'; crons are 'string' (untyped they go json, or array on a comma); never comment the rule at the entry
- [Preview environments infra](project_directus_preview_infra.md) — Preview Admin (runner-hosted ephemeral, cloudflared *.trycloudflare tunnel, url only on run Summary page) auto-triggers on Preview-labeled PR pushes w/ per-PR concurrency; Northflank Preview Deployment webhook DROPPED; **default branch = v11.10.1-hhh-dev since 2026-07-27** (pr-controle retired); pull_request_target resolves from the default = hhh-dev now
- [hiphiphip prod api-guard fake-500](project_hiphiphip_api_guard_fake_500.md) — bo.hiphiphip.app returns FAKE 500s on every /items/* request with a missing/non-allowed Origin or Referer (anti-hacker middleware, NOT an outage); source = @jclaveau/directus-extension-api-guard (planner_2/packages/directus-extension-api-guard/src/api-guard.js), an `authenticate` hook filter gated on API_GUARD_ALLOWED_ORIGINS, skipped when HHH_ENV starts with .env.dev; internal routes (/users, /collections, /utils/cache…) still 200 → send a real browser Origin/Referer when probing
- [Schema-read cache tagging (PR #222)](project_directus_schema_read_cache_tagging.md) — /fields,/relations,/schema/snapshot tagged by their SYSTEM collection (business writes leave them cached); root bug = validateCollection overwrites req.collection on param routes; service emits withMeta tag, controller forwards readMeta; orphan-guard skips tagless collection-less GETs in scoped mode; coarse tag + scoped_cache_fields TODO
- [generateJoi is validation not filtering](reference_directus_generatejoi_not_filter.md) — @directus/utils generateJoi builds a Joi VALIDATION schema, not a Filter→boolean matcher; absent field passes + _null:false polarity diverges; no exported boolean matcher (passesFilter is server-only); don't reuse it client-side (PR #227 regressed 2 tests, reverted)
- [Directus load + infra nets catalog](reference_directus_load_and_infra_nets.md) — check BEFORE hand-rolling: pressure limiter (event-loop, front-door), rate-limiters, DB-pool numPendingAcquires signal, useBus (pub/sub, not durable), @directus/memory (Bus/Kv/Cache/Limiter — NO stream), scheduleSynchronizedJob, useMetrics /metrics prom registry (add labeled counters here); reuse these, hand-roll only the durable stream/buffer/watchdog
- [Build-identity cache flush (PR #285)](project_directus_build_identity_cache_flush.md) — flush redis cache on boot when build identity (api-extension sha1 + baked git commit) changes so a code-only deploy self-heals; closes #282; gated CACHE_AUTO_FLUSH_ON_DEPLOY + redis + cache-enabled; accepted design decisions, don't re-raise
- [Cache TTL rules (PR #279, parked)](project_directus_cache_ttl_rules.md) — per-path/method/query-shape TTL rules replacing single global CACHE_TTL; architecture + descriptor-table-vs-rules-store decision so a fresh review doesn't re-litigate
- [FK error translation (PR #287/#288)](project_directus_fk_error_translation.md) — enriched InvalidForeignKeyError (constraint/relatedCollection/reason/operation, collection:pk, dev-only raw msg); accepted exceptions, don't re-raise
- [85-col cap on .liquid too](feedback_directus_liquid_85_col.md) — jean wants the width gate on .liquid env templates; wired via a no-op-parser block in eslint.style.config.js + `*.liquid` in the gate globs (max-len only, line-based) — not manual
- [Cache vary list order is significant (PR #289)](project_directus_cache_vary_order.md) — CACHE_VARY_CONTENT_TYPES not sorted; order must mirror the endpoint's req.accepts() priority (first = the */* default) or */* callers bucket wrong; settled, don't re-raise "why not sort"
- [Request content-negotiation cache dimensions (PR #289)](project_directus_cache_request_vary.md) — language always-on, content-type json,csv,yaml (xml excluded), header globs + extensible proxy denylist; accepted decisions, don't re-raise; closes #283, follow-up #290
- [Issues auto-close on hhh-dev merge (since 2026-07-27)](project_directus_issue_autoclose_hhh_dev.md) — hhh-dev IS now default → `Closes #N` fires normally on hhh-dev merges; manual close only for PRs merged BEFORE the default flip (e.g. #299/#301)
- [PR #292 cache-hook scopedCache state](project_directus_pr292_cache_hook_scopedcache.md) — context.scopedCache witness matrix (all 4 events + cancels, one file/subject/instance); 2 source fixes (delete DOUBLE-EMIT: fork emit + un-removed upstream emit → double fire, tell = updateMany 1 vs deleteMany 2; cancel-parity: update/delete cancel now drain collector like create); accepted-deferred (side-effecting-cancel no coarse net; #294 event typing)
- [bb M2M nested-write + cancel test design](project_directus_blackbox_m2m_nested_and_cancel.md) — real M2M (CreateFieldM2M, junction FK=`${col}_id`) via parent PATCH `{alias:{create:[{<right>_id:existingId}]}}` fires junction items.create (payload.ts processO2M→createMany) — NOT flat pivot POST; junction scoped_cache_fields+UNIQUE via knex; scoped-instance pattern (redis 6108, seed-then-spawn); REST cancel via allowFilterCancel (null→{data:null}); collection names UNIQUE per file (shared vendor DB)
- [Cache TTL setting + flush multiselect (issue #295, PARKED design)](project_directus_issue295_cache_ttl_flush.md) — persisted `directus_settings.cache_ttl` overriding env (null→env, 0→no-expiry) live via `cacheConfigChanged` bus + hot-path `cacheTtlOverride` mirror; flush button = per-target multiselect (Response ON incl scoped-tag SCAN+DEL gap-fix / System OFF / Locks OFF-warned), per-user localStorage; overlaps #279 cache-ttl-rules (reconcile before building); flushCaches orphans `scalabus:tag:*`
- [PR #306 purgeForMutatedRows](project_directus_pr306_purge_for_mutated_rows.md) — out-of-band scoped-cache purge for raw writes; context-injected not host import; relational fail-safe; merged; #307 read-side parked, #308 seed-retry
- [Local fork api on 8155 (not 8055)](reference_directus_local_api.md) — 8055 belongs to jean's other work; set PORT/PUBLIC_URL in api/.env (.env overrides process env); Vite app on :8080 needs API_URL=http://127.0.0.1:8155; kill the tsx watch parent to stop
- [System-data YAML edits need a dist rebuild](project_directus_system_data_stale_dist.md) — tsx dev api loads packages/system-data/dist/index.cjs not source; symptom = system relations/fields missing locally but present in prod (cache user_id case 08-03); run `pnpm --filter @directus/system-data build`
- [planner cache hit-ratio analysis](project_hiphiphip_cache_hit_ratio_analysis.md) — student_time_slot ~20% hits = query-shape variance (not broken cache); 3 domain callers (listPositionOfSpacedRepetitionTimeSlots _in-list, listCoursePartTimeSlots purged, hasEnoughTimeSlotsToEnableCompanionAutomatically aggregate SDK filter-drop BUG); issue #669
- [Test-run scopes: root `pnpm test` skips api+app](project_directus_test_run_scopes.md) — run api/app explicitly; the full app vitest run OOM-crashes (ERR_IPC_CHANNEL_CLOSED) → shard 4×; `--changed origin/v11.10.1-hhh-dev` is the 13s loop; which 5s-timeout failures are flakes vs a real lazy-import cost
- [v-select's root has no layout box](project_directus_vselect_no_layout_box.md) — the `v-menu` root+activator are 0×0: CSS auto-margins do nothing and Playwright can't click/scroll it; wrap siblings in spans to lay out, click `button.inline-display`
- [PR #326 cache-tree latency percentiles](project_directus_pr326_latency_percentiles.md) — GROUPING SETS per tree level (a rollup of percentiles isn't a percentile), 5 chart-matching metrics, cross-branch band; settled points not to re-raise
- [/server/info is cacheable under FULL purge only](project_directus_server_info_cache_mode.md) — opt-out gated on `scopedCachePurgeEnabled()`; provoke `missing_scope` with `GET /graphql?query={__typename}` (31 bytes) — `/server/specs/oas` is ~150kb so `exceedsMaxSize` wins first
- [Deferred dialects: only postgres gates a PR](project_directus_deferred_dialects.md) — sqlite3/maria covered by standing `ci-dialect-*` branches + PRs #332/#333 refreshed on every hhh-dev tip; trackers #330/#331; 16 jobs queued 310s vs 10 jobs 38s; settled points not to re-raise
- [Blackbox websocket flake + 42703 race](project_directus_blackbox_websocket_flake.md) — `m2o-max-batch` failed because graphql-ws `lazy:true` closed its own socket (code 1000), NOT shard placement; 11 server-spawning suites raced in the parallel middle; transport.ts must keep recording socket error/close
- [Blackbox seeds swallowed failed writes](project_directus_blackbox_silent_write_failures.md) — Create* helpers returned data without checking status → seed-database.test.ts green over a broken schema; dataOrThrow now refuses non-2xx, and the 4 latent defects that surfaced (bigInteger default never applied, temp_relational never relational, wrong pk type behind it, CreateUser not idempotent)
- [PR #353 purge events — settled points](project_directus_pr353_accepted_exceptions.md) — separate table (not a `kind`), `namespace` ≠ `flush` (unbuffered INSERT at mutation rate), entry_tags has no time dimension on purpose, coarse attributes by collection; don't re-raise
- [Slowest-purge filtering needs its own tree](project_directus_purge_tree_deferred.md) — deferred out of #353; a purge isn't an endpoint so its `duration_ms` can't go in the endpoint tree or its metric dropdown; data already stored (`directus_cache_purges` + `_purge_tags`), missing surface is `/utils/cache/purges` + a collection→scope tree
- [Prod DB/Redis access from the laptop](project_directus_prod_db_access.md) — `railway link -w/-p/-e` then `railway run -s <svc> -- psql -f file.sql`; prod PG service is `Postgres-HipHipHip-test`; `-e` required non-interactively; full-keyspace redis SCAN never finishes
- [Keyv raw redis key is doubly namespaced](project_directus_keyv_raw_key_shape.md) — `<ns>_response::<ns>_response:<hash>`; test liveness with `cache.hasMany()`, never a hand-built key
- [PR #396/#398 settled points](project_directus_pr396_settled_points.md) — config_events prefix, `scoped` kept, pending_purges untouched, merged reap job, MAX_BUFFER default, TYPE_MAP sweep scope — do not re-raise

- [Re-dispatch the admin preview without asking](feedback_directus_redispatch_preview_freely.md) — after a push, fire `preview-admin.yml` on the FEATURE branch's ref immediately; cancelling the live tunnel is wanted, not a confirmation-worthy side effect; never PATCH the PR body right after a push (the `edited` run cancels the in-flight preview then skips)
- [PgBouncer admin page (PR #355)](project_directus_pgbouncer_admin_console.md) — knex CANNOT reach the admin console (`select version();` on connect → rejected, use a bare pg.Client); bundling `pg` drags in pg-native → api dies at boot (tsdown `external`); SHOW POOLS `database` vs SHOW DATABASES `name`; `application_name = directus:<nodeId>:<connection>`; settled: PGBOUNCER_* derived from the connection registry, no persistence, pg-only, read-only
- [Blackbox: observing a saturated pgbouncer pool](project_directus_blackbox_pgbouncer_saturation.md) — `/pools-under-load` answers ~400ms in and holds its sleeps AFTER (await it to OPEN the window); supertest never sends an un-awaited request; poll for `serversActive>0`, and assert the queue via cumulative `total_wait_time`, never `cl_waiting`
- [Scalabus licensing](project_scalabus_licensing.md) — fork additions = jean's own BSL-1.1, Additional Use Grant None, Change Date 4y; scope is repository-minus-upstream NEVER branch names; Licensor is jean personally, `license` stays Monospace's; PolyForm Shield or GPLv3 is a deliberate later decision
- [Derived branches regenerate themselves](project_scalabus_derived_branches.md) — `*-dist` (release-fork.yml) and `ci-dialect-*` (refresh-dialect-ci.yml) rebuild from the trunk via `checkout -B` on every push; never rewrite, hand-edit or delete them; `-dist` ships api/src and is what the planner installs
- [Cache key ignores unknown query params](project_directus_cache_key_ignores_unknown_params.md) — key = pathname + sanitizedQuery + user; sanitizeQuery copies only known params, so `?myKey=a|b` collapses to ONE entry (vary `fields` instead); no IP in the key
- [The admin preview is not a CI check](feedback_directus_ci_watch_skip_preview.md) — meant to run until its worker timeout or until a newer preview supersedes it; never wait on it AND never mention it in a CI report whatever state it is in (not even as an exception); judge on Check/Style/CodeQL, read the URL from the PR description
- [Codecov patch can be stale two ways](project_directus_codecov_stale_flag.md) — a conflicting PR stops `pull_request` CI (only `pull_request_target` fires, so CI looks alive); and a flag upload can carry forward while green — tell is an identical percentage, fix is rerunning the uploading job
- [Publishing a path in the OpenAPI](project_directus_oas_publishing.md) — `/server/specs/oas` is unauthenticated and gates per TAG (`x-authentication`), env-gated routes still appear (document the 404), spec is OAS 3.0.1 so `nullable: true` not `type: null`
- [PR #350 system MCP — settled points](project_directus_pr350_accepted_exceptions.md) — MERGED 2026-08-13; foreign-spec names unprefixed, 2025-03-26 refused on purpose, #352 blast radius, barrel 0% artefact, namespace reach joins nothing BY DESIGN, listing `purges` excludes clears, REST `?window=` now 400s; don't re-raise
- [Worktrees share the main tree's node_modules](project_directus_worktree_shared_node_modules.md) — `.wt-*/{,api/}node_modules` symlink to `directus-cms-2`, so `@directus/*` resolves from whatever branch the MAIN tree is on; cross-package tests fail locally, pass in CI; baseline-diff before blaming your diff
- [Cache key identities: cacheKey vs redisKey](project_directus_cache_key_identities.md) — TWO permanent identities (digest = every table's join key + descriptors PK; redisKey = what Redis is keyed by, readable under CACHE_KEY_HASH_ENABLED=false); neither legacy; /utils/cache/entry + evict + MCP take redisKey; a wrong one fails SILENTLY as a cold cache
- [cache-events.test.ts shares ONE mock builder](project_directus_cache_events_mock_builder.md) — resolved by `lastTable` at await time, so two builders held before either awaits both read the last table; never Promise.all two queries there; real SQL belongs in blackbox
- [Postgres only in the main workflow](feedback_directus_pg_only_dialect_focus.md) — don't invest in MariaDB/SQLite for now (revisit in months); the bb PR matrix is already postgres-only by design, so never report their absence as a gap
- [PR #358 pk-pinning — settled points](project_directus_pr358_accepted_exceptions.md) — one goal so the 4 commits stay together, batch fan-out accepted uncapped (imports only; Promise.all → ~1 RTT, awaited in the mutation), self-ref collection DELETE 500s → DeleteField first; don't re-raise
- [api build needs the copyfiles step](project_directus_api_build_needs_copyfiles.md) — bare `tsdown` cleans dist and wipes copied .yaml/.liquid/.md, then bootstrap dies with a misleading `no such table: directus_migrations`
- [Redis outage kills the API process](project_directus_redis_outage_kills_process.md) — 5 independent unhandled emitters/rejections in v11.10.1 (shared ioredis client, bus `duplicate()`, 4 Keyv node-redis clients, every scheduled tick, +1 unfound); all upstream-shaped; fixed on the pk-pin branch
- [The blocking codecov check is the unflagged patch at 95%](project_directus_codecov_aggregate_patch_gate.md) — every per-package flag is `informational: true`; blackbox feeds the aggregate but the bundled build credits only the FIRST line of a multi-line statement, so bb-only code lands ~20% and only unit tests clear the bar
- [PR #366 redis outage resilience](project_directus_pr366_redis_resilience.md) — split out of #358 and merges BEFORE it; 5 listeners + log throttling + `disableOfflineQueue` (surviving ≠ answering); `cache-stats` `*/10 * * * * *` is the only sub-minute schedule; deliberate coverage gaps listed
- [Diverging from upstream files is fine here](feedback_directus_diverge_from_upstream_freely.md) — "it would touch a pristine upstream file" is NOT a reason to decline a fix; AGENTS.md's minimal-diff rule is about not reformatting untouched lines
- [Purge recovery vs the bare collection tag](project_directus_purge_recovery_bare_tag.md) — #365's recovery drains exactly what it recorded, and the recorded set includes the bare collection tag → the whole collection is purged; open question on whether a single-row PATCH should record it
- [Cache-stats prod telemetry loss (2026-08)](project_directus_cache_stats_prod_incident.md) — the 0x00 escaping drops (483 batches / 75k events, 1-2%) AND the 55h autokill blackout (~2.2M events, 29x bigger); #372's "since #370" causality was WRONG and is corrected; issues #375/#376
- [Cache-stats table roles](project_directus_cache_stats_table_roles.md) — both scoped_cache tag tables are pure telemetry (Redis `scalabus:tag:*` is the mechanism, sole reader is the cache page's purge-coverage join); everything IS reaped; cache_events+cache_purges are Timescale hypertables; the byte budget measures only cache_events
- [Planner pins the fork by branch but LOCKS a commit](project_planner_directus_dist_pin.md) — `--frozen-lockfile` means a redeploy ships nothing; re-resolve `pnpm-lock.yaml` first; how to read what prod actually built
- [PR #371 cascade/nullify purge — settled points](project_directus_pr371_accepted_exceptions.md) — MERGED; two goals in one PR accepted, coarse bare tag by design, SET NULL doesn't propagate, self-relation exempt only when it rewrites; don't re-raise
- [listCacheEntries perf, measured](project_directus_cache_entries_query_perf.md) — never hit the statement timeout; the 13-column GROUP BY is NOT what spills (131 vs 130 MB); and the window DEFAULT never reaches the page (cache.vue always sends ?window=, picker defaults 24h) — refutes #377
- [PR #377 cache-entries listing — settled points](project_directus_pr377_accepted_exceptions.md) — MERGED 4682664f45; refuted-claim correction is deliberate, two-read race + listedKeys superset + clamp asymmetry all accepted; the CTE and the missing latencies LIMIT are still jeans call
- [PR #373 single-use lint rules — settled points](project_directus_pr373_accepted_exceptions.md) — MERGED 004a131d1e; exports are names not uses, a template mention disqualifies, typeof counts, name-based skip has a pinned blind spot; both rules are `warn` and warn gates NOTHING
- [PR #387 updateGroups event shape](project_directus_pr387_update_groups.md) — grouped `items.update` + per-row `items.update.one`, what a hook migration looks like, and the points already ruled on (don't re-raise)
- [Cache layers fail differently](project_directus_cache_failure_semantics.md) — ioredis raises, Keyv swallows, the bus is fire-and-forget, @directus/memory raises; one "Redis is down" per layer
- [#392 purge fan-out is unbounded](project_directus_issue392_purge_fanout.md) — one tag per key since #358; a large failed purge cannot even be recorded; N awaiting jean
- [PR #393 accepted exceptions](project_directus_pr393_accepted_exceptions.md) — nested/parent vocabulary, settled names + type calls, review defects already fixed; do NOT re-raise
- [Scoped-cache pin soundness](project_directus_scoped_cache_pin_soundness.md) — the subset invariant and the two query shapes that break a key pin (root filter/sort, deep-filtered parent)
- [Issue #397 unbounded spread](project_directus_unbounded_spread_397.md) — `f(...arr)` RangeError, 58 sites, every batch-create response path
- [knex-mock matches raw SQL](reference_directus_knex_mock_sql_matching.md) — a column name steals another table's query; register the narrower handler first; RELATIONAL_BATCH_SIZE loop
- [Prod scoped-cache Redis, measured](project_directus_prod_cache_measurement.md) — 2026-08-26: 270k keys/218MB, maxmemory 0+noeviction, 130 B per slice-index member, effective CACHE_TTL 48h (settings override) not the env's 1h; zero pk-shaped tags does NOT mean #358 is absent
- [Run ONE blackbox file locally](project_directus_blackbox_single_file_locally.md) — the completion barrier hangs silently; needs `--shard=1/1` + the six `before` files (seed-database gives the admin token) or every request 403s
- [Filter normalization happens at three layers](project_directus_filter_normalization_layers.md) — only parseFilter is shared, so the AST describes a different query than the one that runs; root cause of the #402 tag divergences, plus the fields=* / cache-key traps
- [An M2O filtered on its related key needs no tag](project_directus_m2o_filter_needs_no_tag.md) — behind an enforced FK no write to the far collection can change the read; gated on relation.schema, three load-bearing guards
- [PR #402 accepted exceptions](project_directus_pr402_accepted_exceptions.md) — scope is "avoid bare tags NOTHING ELSE"; kept warnings, _not handling, folded-in #404; do NOT re-raise
- [Classify cached reads by filter shape](project_directus_cache_descriptor_query_analysis.md) — directus_cache_descriptors.query holds the raw query string; join directus_relations to tell aliases from scalars; prod SQL is CTO-gated and classifier-blocked
- [Split coverage by which suite can move it](project_directus_coverage_bb_vs_unit_split.md) — per-flag line report tells a bb-reachable line from one only a unit test credits; bb ceiling is far below the 95% gate
- [Filter shapes not reachable over HTTP](project_directus_filter_shapes_not_http_reachable.md) — `_not` and empty `_in` are 400s from validate-query, relational groupBy 500s; unit-test them instead
- [Isolate new blackbox fixtures](project_directus_bb_fixture_isolation.md) — `fields=*` splices every alias, so a new relation joins every read in the file; give new scenarios their own collections
- [The `dist-release` PR label](project_directus_dist_release_label.md) — builds `<branch>-dist` for a PR head; head.sha/head.ref/same-repo gate, and the delete-recreate race it exposed
- [Migration runs are transactional on Postgres only](project_directus_migration_transaction_design.md) — sqlite's alter-table rebuild kills savepoints, MySQL DDL implicit-commits; five error-swallowing core migrations must stay outside (PG aborts on any error); `transactionScope` batch|own|none; settled points for #420/#421
- [Health holds while migrations are outstanding](project_directus_health_holds_on_outstanding_migrations.md) — 503 rather than refusing to listen (Railway gates switchover; a cron restart would otherwise take the service down); watch starts pessimistic and stops on first clean read; settled points for #403/#421
- [Blackbox can spawn its own instance](project_directus_blackbox_spawn_own_instance.md) — `spawn(cli start)` on a free port with a custom `EXTENSIONS_PATH`/`MIGRATIONS_PATH` is the only way to test boot-state; readiness is `/server/ping`, not `/server/health`
- [Planner's Railway deploy shape for this fork](project_directus_planner_deploy_shape.md) — API container never migrates, `PM2_INSTANCES=1` then autoscale to 32, autorestart on, `healthcheckPath: /server/health`, nightly `cron_restart`
- [Dialect coverage lands on the ci-dialect branches](project_directus_trunk_full_vendor_matrix.md) — a PR runs postgres only, a trunk push runs NO blackbox at all; sqlite3/maria appear on the refreshed `ci-dialect-*` branches, and their red is out of scope
- [app.test.ts cannot pass locally](project_directus_local_api_test_env.md) — it needs a built `@directus/app`, whose build dies with SIGHUP under the Bash tool; read the failure text before calling any api-suite failure environmental
