type: object
description: >
  The autoscale configuration sharedConfig and the supervisor options, which are
  kept under separate keys and written through separate routes.
properties:
  key:
    type: string
  sharedConfig:
    $ref: '../openapi.yaml#/components/schemas/AutoscaleSharedConfig'
  setByEmail:
    type: string
    nullable: true
  supervisor:
    $ref: '../openapi.yaml#/components/schemas/AutoscaleSharedConfigAnswer'
